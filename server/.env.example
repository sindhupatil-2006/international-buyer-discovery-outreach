# Backend Environment Configuration Example
PORT=5000

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=buyer_discovery
DB_USER=root
DB_PASSWORD=

# JWT Authentication Secret
JWT_SECRET=super_secret_jwt_key_buyer_discovery_portal_2026

# External API Keys (Optional - Demo mode activates if missing)
SERPAPI_KEY=
GEMINI_API_KEY=

# Nodemailer SMTP Configuration (Optional - Demo mode activates if missing)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Frontend Client URL (CORS)
CLIENT_URL=http://localhost:5173
